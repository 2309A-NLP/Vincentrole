# 部署验证记录

## 测试环境

| 项目 | 内容 |
|------|------|
| 宿主机 | macOS 26.5, Apple M4, 24GB RAM |
| Docker | Docker Desktop, aarch64 |
| 镜像 | finance-qa:latest (3.39GB) |
| 容器名 | finance-qa-test |
| 端口 | http://localhost:8520 |
| 测试时间 | 2026-06-09 |

## ✅ 验证项

### 1. 构建成功
```bash
$ docker build -t finance-qa:latest ./
```
✅ 镜像构建成功，大小 3.39GB

### 2. 容器启动
```bash
$ docker run -d --name finance-qa-test -p 8520:8501 finance-qa:latest
$ docker ps | grep finance-qa
```
✅ 容器状态: **Up 24 minutes**

### 3. 服务可达
```bash
$ curl -s -o /dev/null -w "HTTP %{http_code}" http://localhost:8520
```
✅ **HTTP 200**

### 4. RAG 问答测试（容器内执行）

| 问题 | 回答 | 结果 |
|------|------|:----:|
| 注册资本是多少？ | 6,000万元 | ✅ |
| 法定代表人是谁？ | 【演示模式】 | ⚠️ 未配置 API Key |
| 军用领域收入占比？ | 85%以上 | ✅ |

### 5. 检索质量
| 指标 | 结果 |
|------|:----:|
| FAISS 索引加载 | 5833 条 ✅ |
| 单次检索结果 | 5 条向量 + 5 条关键词兜底 |
| 检索耗时 | ~0.5s |

## 总结

**部署成功。** 容器正常运行，RAG 问答系统可通过 http://localhost:8520 访问，配置 API Key 后可正常使用 LLM 生成答案。
